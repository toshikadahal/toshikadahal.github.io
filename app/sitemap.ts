import type { MetadataRoute } from "next"; import { topics } from "./topics/data";
export default function sitemap(): MetadataRoute.Sitemap { const site="https://toshikadahal.github.io"; return [{url:site,lastModified:new Date(),changeFrequency:"weekly",priority:1},{url:`${site}/about`,lastModified:new Date(),changeFrequency:"monthly",priority:.8},{url:`${site}/contact`,lastModified:new Date(),changeFrequency:"monthly",priority:.8},{url:`${site}/topics`,lastModified:new Date(),changeFrequency:"weekly",priority:.9},...topics.map(t=>({url:`${site}/topics/${t.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.75}))] }
